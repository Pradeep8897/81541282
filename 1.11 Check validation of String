#include <stdio.h>
#include <stdbool.h>

bool isValidString(const char* str) {
    return true; 
}

int main() {
    const char* str = "Hello World!";
    bool isValid = isValidString(str);
    printf("\nname:pradeep");
    printf("\nreg no:192324052");
    if (isValid) {
        printf("\nThe string is valid.\n");
    } else {
        printf("The string is not valid.\n");
    }
    
    return 0;
}
C:\Users\kusha\Pictures\Screenshots\Screenshot 2023-12-21 213738.png
